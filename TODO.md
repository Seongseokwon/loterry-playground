# TODO

기준일: 2026-08-12 · Next.js 16.2.6 / React 19.2.6
데이터: 제1~1236회 (2026-08-08 추첨분까지)
백엔드 설계: **`BACKEND-PLAN.md`** (Prisma 7 + Prisma Postgres)
기준 커밋: `2980c19` (Connect next pattern to number drawing)

**진행 상황** — A 완료 / B-1~B-4 완료 / B-5는 4항목 완료·2항목 E-4 대기 / B-7 완료 / **E-1 완료** / **E-2는 `/admin/draws`와 실운영 검증만 남음** / 남은 것은 B-5 잔여, B-6, B-8, C-2, D, E-3, E-4, E-6

---

## A. 마이그레이션 잔재 정리 — 완료

Next.js 전환 후 남은 것들. 코드 동작에는 영향 없지만 리포지토리를 지저분하게 만들고, 라인엔딩 문제는 앞으로의 모든 diff를 오염시킨다.

### A-1. 라인엔딩 정규화

- [x] `.gitattributes` 추가 — `* text=auto eol=lf`
- [x] `git add --renormalize .` 실행 후 단독 커밋

### A-2. 빈 디렉토리 삭제

- [x] `worker/` — Cloudflare Worker 진입점 (미사용)
- [x] `db/`, `drizzle/` — Drizzle ORM 잔재. ORM은 **Prisma로 확정** (`BACKEND-PLAN.md`)
- [x] `mocks/` — MSW 잔재
- [x] `build/` — 구 빌드 출력
- [x] `app/_sites-preview/` — vinext 프리뷰 라우트

### A-3. 구 런타임 산출물·설정

- [x] `.vinext/` / `.wrangler/` / `dist/` 삭제
- [x] `.openai/hosting.json` 처리 — Vercel로 배포처 확정

### A-4. 설정 마무리

- [x] `next.config.ts` 옵션 검토
- [x] `pnpm-workspace.yaml` 정리
- [x] `pnpm build` / `pnpm test` / `pnpm lint` 3종 통과

---

## B. Phase 2 (v1.1) 기능 개발

기획서 §3.3.2 / §3.4 / §3.5 기준.

### B-1. 고급 조건 UI 노출 — 완료

- [x] "고급 조건" 아코디언 컴포넌트 (기본 접힘)
- [x] 홀짝 비율 `oddCount` / 고저 비율 `lowCount` 0~6 세그먼트
- [x] 합계 구간 `sumRange` 프리셋 + 직접 입력(21~255)
- [x] 끝수 분산 `maxSameTail` 1개 / 2개
- [x] 조건 충돌 시 `relaxed` 배지 완화 안내
- [x] `tests/draw-engine.test.ts` 4종 조건 케이스

### B-2. 보관함 (IndexedDB) — 완료

- [x] `lib/storage.ts` — IndexedDB 래퍼 (최대 50세트)
- [x] 저장 스키마: 번호 6개, 생성 조건 스냅샷, 라벨/메모, 대상 회차, 생성 시각
- [x] `/archive` 라우트 + 헤더·푸터 내비게이션
- [x] 저장 버튼 실제 연결 + 라벨 입력
- [x] 회차 확정 시 자동 등수 판정 → 보관함 배지 (`lib/rank.ts` 재사용)
- [x] 50세트 초과 처리 정책 / SSR 가드
- [x] **동기화 대비 스키마 보강** — `SavedSet`에 `updatedAt`·`deletedAt` 추가, `DB_VERSION` 1→2, `deleteSavedSet()` 툼스톤 마킹 (커밋 `e59380b`)

### B-3. 궁합수 — 완료

45C2 = 990쌍 동시출현 집계. (기존 1,035 표기는 46C2와 혼동한 산술 오류.)

- [x] `lib/stats.ts` `pairStats` 집계 (990쌍, 모듈 초기화 시 1회 계산)
- [x] `/draw/pair` 프리셋 라우트 + `generateStaticParams`
- [x] 기준 번호 1~2개 선택 UI, 상위 K개 파라미터
- [x] 통계 화면 궁합수 Top 10 바 차트
- [x] 고지 카피 — 독립 시행 오해 방지 (기획서 §3.3.6)

### B-4. 기념일 — 완료

- [x] 날짜 다중 입력 UI → 1~31 매핑, 부족분도 1~31 안에서 랜덤 채움
- [x] `/draw/birthday` 프리셋 라우트
- [x] 편향 안내 카피 — ① 32~45 통째 배제 ② 당첨금 분할 확률

### B-5. 통계 대시보드 확장 — 4/6

- [x] 홀짝 / 고저 분포 — 스택 바 (최근 10/50/100/전체/직접 지정)
- [x] 합계 분포 — 히스토그램 + 중앙값 마커, 구간 선택 → 해당 회차 목록
- [x] 1등 당첨금 추이 — 라인 차트
- [x] 번호 상세 시트에 궁합 Top 5
- [ ] 등위별 당첨금 — 수집기 확장 필요. **E-4의 `DrawPrize`와 같이 움직인다**
- [ ] 1등 배출 판매점 — 공공데이터포털 「기획재정부_온라인복권 1등 당첨 판매점 현황」 개방 데이터 (크롤링 아님). **E-4의 `WinningStore`와 같이 움직인다**

### B-6. QR 스캔 — 미착수

- [ ] **실물 용지 파싱 검증 선행** — 이게 안 되면 나머지는 무의미
- [ ] QR 페이로드 포맷 분석 (동행복권 용지 URL 구조)
- [ ] 카메라 권한 UX + 실패 시 수동 입력 폴백
- [ ] `components/check/CheckPanel.tsx`에 통합

### B-7. 다음 패턴 추천 — 완료 (문서에 뒤늦게 반영)

기획서에 없던 기능인데 커밋 3개(`4799431` → `f3600f2` → `2980c19`)로 들어갔다. 최근 100회의 번호대별 분포 패턴을 세어 다음 회차에 나올 법한 패턴을 제시하고, 그 패턴을 추첨 조건으로 연결한다.

- [x] `lib/next-pattern.ts` — `analyzeNextPatterns()`, 윈도우 100회, 번호대 5구간
- [x] 추천 진입 UI + 추첨 엔진 연결
- [x] `tests/next-pattern.test.ts`
- [x] 고지 카피 — `NextPatternPreview.tsx:49`, `DrawBuilder.tsx:183` 두 곳에 "당첨을 예측하지 않는다" 문구 있음
- [ ] **기능 이름 재검토** — 카피는 있지만 "다음 패턴 추천"이라는 **이름 자체**가 예측력을 암시한다. 사용자는 본문보다 제목을 기억한다. "최근 패턴 살펴보기" 류로 바꿀지 판단 (기획서 §3.3.6)
- [ ] 기획서 §3.3에 기능 항목으로 역반영

### B-8. 이번주 생성 번호 통계 — 신규, 미착수

**아이디어**: 사용자들이 이 사이트에서 만든 번호를 회차 단위로 집계해 "이번 주 많이 나온 번호"를 보여주는 화면. 회차가 확정되면 실제 당첨 번호와 겹쳐 보여준다.

이건 우리만 가진 데이터라 다른 로또 사이트가 복제할 수 없고, 토요일 밤 재방문 훅이 된다. 다만 **지금 구조에서 공짜가 아니다.** 아래 세 가지를 먼저 정하고 들어갈 것.

#### 결정 1 — 무엇을 집계할 것인가 (비용에 직결)

현재 `/draw`는 **DB 접근 0건**이고 그게 무료 티어 방어책의 핵심이다(§E-5, `BACKEND-PLAN.md` §8). 생성마다 쓰기를 하면 이 원칙이 깨진다.

| 안 | 쓰기량 | 데이터 품질 | 판단 |
|---|---|---|---|
| 생성할 때마다 기록 | 사용자 1명이 10번 돌리면 10건 | 노이즈 많음 — 마음에 안 들어 버린 번호까지 집계됨 | 비쌈 |
| **보관함에 저장할 때만 기록** | 저장 1건당 1건 | **사용자가 실제로 고른 번호만** | **권장** |
| 클라이언트에서 배치 전송 | 세션당 1건 | 중간 | 구현 복잡도만 늘어남 |

→ **"저장할 때만"이 비용도 싸고 의미도 낫다.** "이 번호로 하겠다"고 결정한 것만 세는 편이 통계로서 정직하다. 이걸 기본안으로 잡되 착수 시 재확인.

#### 결정 2 — 프레이밍 (여기가 제일 중요)

"많이 뽑힌 번호"를 그냥 보여주면 밴드왜건이 생기고, 기획서 §3.3.6 정직성 원칙에 정면으로 걸린다. 로또는 독립 시행이라 **인기 번호는 당첨 확률을 1도 올리지 않는다. 올리는 건 당첨 시 당첨금이 쪼개질 확률뿐이다.**

그래서 이 화면의 카피는 "이 번호가 유망하다"가 아니라 **"이 번호는 겹칠 확률이 높다"** 로 가야 한다. B-4 생일 편향 안내와 정확히 같은 논리이고, 그렇게 쓰면 오히려 정직성 원칙을 **강화**하는 기능이 된다. 이 프레이밍을 놓치면 만들지 않는 편이 낫다.

- [ ] 카피 초안 확정 후 구현 착수 — 순서를 바꾸지 말 것

#### 결정 3 — 어뷰징

집계 엔드포인트가 공개되면 봇이 특정 번호를 밀어 넣어 통계를 왜곡할 수 있다. 화면에 "N명이 골랐다"가 뜨는 순간 조작 동기가 생긴다.

- [ ] IP/세션 단위 rate limit + 회차당 제출 상한
- [ ] 표본이 적을 때(예: 100세트 미만) 순위 대신 "표본 부족" 표시 — 초기에 소수 제출로 순위가 요동치는 걸 막는다

#### 화면

- [ ] `/stats/weekly` 또는 `/stats` 내 탭 — 라우트 위치 결정
- [ ] 번호별 선택 횟수 Top 10 + 45칸 히트맵 (기존 `StatsPanel` 히트맵 재사용)
- [ ] 총 생성 세트 수, 참여 규모 표시
- [ ] 조건별 분포 — 프리셋(핫넘버/궁합수/기념일/다음패턴) 사용 비율
- [ ] **회차 확정 후 비교 뷰** — 실제 당첨 번호 6개를 히트맵에 겹쳐 표시. "가장 많이 고른 번호 중 몇 개가 맞았나"가 이 기능의 하이라이트다
- [ ] 지난 회차 아카이브 (회차 셀렉터)

#### 데이터

- [ ] **E-6** 참고 — `GeneratedPick` 모델과 집계 파이프라인

#### 그 외 확인

- [ ] 개인정보처리방침(D)에 "생성 번호의 익명 집계" 문구 추가 — 사용자 식별자는 저장하지 않는다
- [ ] 비로그인 사용자 기록에 대한 옵트아웃 필요 여부 판단

---

## C. 착수 전 확인 필요

### C-1. 정적 생성 범위 — 완료

기획서 §9는 최근 104회만 정적 생성 + 색인하도록 설계했다. 기존 `generateStaticParams`는 1,236회를 전부 생성해 빌드 시간·컴플라이언스 양쪽에서 문제였다.

- [x] 최근 30회만 정적 생성·색인, 이전 회차는 요청 시 생성 + `noindex` (커밋 `7f0c30b`)

### C-2. 없는 것들 — 미착수

- [ ] `app/sitemap.ts` — 미구현
- [ ] `app/robots.ts` — 미구현
- [ ] 구조화 데이터 (`Article`, `BreadcrumbList`)
- [ ] 프리셋 페이지 설명 콘텐츠 200자 이상 (SEO)

---

## D. 운영 전

- [ ] 동행복권 데이터 이용 문의 및 법률 검토 (기획서 §7.1 — 저작권법 제93조)
- [ ] 이용약관, 개인정보처리방침, 면책 문구 검토 — **B-8 착수 시 생성 번호 익명 집계 문구 포함**
- [x] 데이터 주간 자동 갱신 → E-2로 이관, 구현 완료
- [ ] 실데이터로 핫넘버 가중치 튜닝
- [ ] 계정 동기화 → E-3으로 이관
- [ ] 웹 푸시, 분석 이벤트 (v1.2)

---

## E. 백엔드 (Prisma 7 + Prisma Postgres)

상세 설계는 **`BACKEND-PLAN.md`**. 여기는 체크리스트만 둔다.

> Prisma 7은 v6와 설정 방식이 크게 다르다 — generator가 `prisma-client`로 바뀌고, `output` 지정이 필수가 되고, 드라이버 어댑터가 필수가 되고, DB URL이 `prisma.config.ts`로 이동했다. 인터넷 예제 대부분이 v6 기준이라 그대로 따라가면 깨진다. `BACKEND-PLAN.md` §2 대조표를 먼저 볼 것.

### E-1. 읽기 경로 이관 — 완료 (커밋 `ea82e91`, `dbd6f52`)

- [x] `prisma` `@prisma/client` `@prisma/adapter-pg` `pg` `dotenv` `tsx` 설치 (7.9.1)
- [x] `prisma.config.ts` + `.env` — 로컬은 `docker-compose.yml`의 postgres:16-alpine
- [x] `tsconfig.json` target `ES2023`
- [x] `.gitignore` / eslint ignore에 `lib/generated/` 추가
- [x] `package.json` build → `prisma generate && next build`
- [x] `lib/prisma.ts` 싱글턴 (`PrismaPg` 어댑터 + `globalThis` 캐싱)
- [x] `Draw` / `NumberStat` 모델 + 마이그레이션 `20260812051506_init`
- [x] `prisma/seed.ts`로 JSON 1,236회 이관 (재크롤링 없음)
- [x] `lib/repositories/draws.ts` — `getDraws` / `getDrawByRound` / `getNumberStats`
- [x] `/results`, `/results/[round]`, `/stats` 전환
- [x] `data/lotto-draws.json` 존치 — **삭제하지 말 것. 되돌릴 수 있는 마지막 지점**

> `lib/draw-engine.ts`, `lib/rank.ts`, `lib/stats.ts`는 순수 함수로 유지됐다. 이 상태를 깨지 말 것 — `/draw`·`/check`의 DB 접근 0건이 여기서 나온다 (E-5).

### E-2. 수집 자동화 — 거의 완료 (커밋 `f3600f2`)

- [x] 파싱·검증 로직을 `lib/collector/source.mjs`로 추출 (스크립트와 공유)
- [x] `POST /api/internal/collect` — `CRON_SECRET` Bearer 검증, 멱등 처리
- [x] `.github/workflows/collect.yml` — `*/10 12-14 * * 6` (KST 토 21:00~23:59)
- [x] `revalidateTag("draws")` / `revalidateTag("stats")` 연결
- [x] `CollectionLog` 모델 + 마이그레이션 `20260812052649_add_collection_log`
- [ ] **GitHub Secrets 등록 확인** — `COLLECT_URL`, `CRON_SECRET`. 워크플로가 둘 다 없으면 즉시 실패하게 짜여 있으니 등록 안 됐으면 조용히 안 도는 게 아니라 매주 실패 알림이 온다
- [ ] 4xx/429 시 자동 중단 + **운영자 알림 채널 연결** (**우회 금지** — 기획서 §7.1). 현재 `CollectionLog`에 기록만 하고 알림이 없어 실패를 아무도 모른다
- [ ] `/admin/draws` 수동 입력 폼 — 수집 실패 백업책 (기획서 §4.1). E-2에서 유일하게 손도 안 댄 항목
- [ ] **다음 토요일 실제 추첨으로 무인 검증** — 이게 끝나야 E-2 마감
- [x] `scripts/collect-lotto-draws.mjs`는 백필 전용으로 존치

### E-3. 인증·동기화 (약 1.5주) — 미착수

- [x] B-2 스키마 보강 선행 (`updatedAt`·`deletedAt`·툼스톤)
- [ ] Better Auth 설치 (**Auth.js 아님** — 2025년 9월 팀 합류 후 신규 프로젝트는 Better Auth 권장, 두 스키마는 호환 안 됨)
- [ ] 로그인 1종 (이메일 매직링크 또는 카카오)
- [ ] `SavedTicket` 모델 — `lib/storage.ts`의 `SavedSet`과 1:1
- [ ] 동기화 엔드포인트 3종 (`GET /api/tickets`, `POST /api/tickets/sync`, `DELETE /api/tickets/:id`)
- [ ] 병합 규칙 구현 + 테스트: 오프라인 생성 → 로그인 → 병합 / 양쪽 수정 충돌 / 삭제 후 재동기화
- [ ] **최초 로그인 시 익명 티켓 귀속** — 빠뜨리면 "로그인했더니 번호가 사라졌다"
- [ ] IndexedDB를 1차 저장소로 유지 (서버는 백업·동기화)

### E-4. v1.1 데이터 — 미착수

- [ ] `DrawPrize` 등위별 당첨금 — 별도 페이지 파싱 (B-5 잔여와 연계)
- [ ] `WinningStore` — 공공데이터포털 개방 데이터 (B-5 잔여와 연계)
- [ ] `PairStat` 테이블은 **만들지 않는다** — `lib/stats.ts` 런타임 계산(990쌍)으로 충분. 빌드 시간 실측에서 문제될 때만 재검토

### E-5. 비용 방어

- [ ] 무료 티어는 월 100k 오퍼레이션 / 500MB. **TCP 직결은 요청당 1건 과금**
- [ ] `/draw`·`/check`·`/archive`는 DB 접근 0건 유지 — **B-8이 이 원칙을 건드리는 첫 기능이다**
- [ ] 첫 달 실사용량 측정 후 플랜 판단

### E-6. 생성 번호 집계 (B-8 백엔드) — 신규, 미착수

B-8의 결정 1~3이 끝난 뒤 착수한다. 화면부터 만들면 스키마를 두 번 짠다.

- [ ] `GeneratedPick` 모델 — `targetRound`, `numbers Int[]`, `presetId`, `createdAt`. **사용자 식별자·IP를 저장하지 않는다** (rate limit은 저장 없이 메모리/엣지에서)
- [ ] `WeeklyPickStat` 집계 테이블 여부 결정 — 제출이 쌓이면 매 페이지뷰마다 전량 스캔은 못 한다. 회차별 45행으로 미리 접어두는 편이 낫다
- [ ] `POST /api/picks` — 저장 시점에 1건 기록, 회차당 제출 상한
- [ ] 집계 갱신 주기 결정 — 실시간 vs N분 캐시. `revalidateTag("picks")` + `unstable_cache`로 페이지뷰당 DB 접근을 끊을 것
- [ ] E-2 수집 핸들러에 회차 확정 시 집계 확정(freeze) 단계 추가 — 확정 후에는 다시 계산하지 않는다
- [ ] **비용 재추정** — 무료 티어 100k/월 기준으로 주당 몇 건까지 감당되는지 계산하고 상한을 그 아래로 잡을 것. 이 숫자를 안 내고 열면 토요일 밤에 티어가 터진다
- [ ] 오래된 원본 `GeneratedPick` 정리 정책 (집계 확정 후 N주 뒤 삭제) — 500MB 스토리지 방어

---

## 권장 순서

A / B-1~B-4 / B-7 / C-1 / E-1 완료. 남은 것의 순서:

1. **E-2 마감** — GitHub Secrets 확인 → `/admin/draws` 폼 → 다음 토요일 무인 검증. 여기까지 와야 데이터가 손 안 대고 갱신된다
2. **C-2** sitemap·robots — 의존성 없고 배포 전 필수. 짬 날 때
3. **B-8 결정 1~3** — 코드 한 줄 없이 문서에서 끝나는 일. 여기서 프레이밍을 정해두면 나머지가 쉬워진다
4. **E-6 + B-8 화면** — 스키마 먼저, 화면 나중
5. **E-4 + B-5 잔여** 등위별 당첨금·판매점 — 테이블과 화면을 같이
6. **E-3** 인증·동기화 — 작업량이 가장 크고 없어도 서비스가 돈다
7. **B-6** QR — 실물 용지 파싱 검증 결과에 따라 통째로 드롭될 수 있음

**B-7 고지 카피 점검은 아무 때나, 그러나 반드시.** 이미 배포된 기능에 붙는 문제라 우선순위가 낮아 보이지만 정직성 원칙 위반은 기능 미구현보다 비싸다.
